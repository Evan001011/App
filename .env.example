# Studently Environment Variables

# Database (Required)
# PostgreSQL database connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/studently

# Individual database connection details (auto-populated by Replit)
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=studently

# AI Integration (Required)
# Get your free API key at https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Session Security (Required)
# Generate a random string for session encryption
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_secret_key_here
